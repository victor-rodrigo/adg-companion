{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main ADG Companion window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
